{
  "name": "bewd_ca1",
  "version": "1.0.0",
  "description": "const express = require('express');\r const app = express();\r const data = require(\"./MOCK_DATA.json\");\r const port = 3000;",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.9.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
